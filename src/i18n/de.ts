import home from '../content/home/de.json';
import calendar from '../content/calendarDE.json';
import type { LocaleStrings } from './types';

const strings = {
  home,
  calendar,
} satisfies LocaleStrings;

export default strings;

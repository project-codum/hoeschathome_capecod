import home from '../content/home/en.json';
import calendar from '../content/calendarEN.json';
import type { LocaleStrings } from './types';

const strings = {
  home,
  calendar,
} satisfies LocaleStrings;

export default strings;
